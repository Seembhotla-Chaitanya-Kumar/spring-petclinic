apiVersion: v1 
kind: Service
metadata:
  name: spring-service

spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30005
  
  selector:
    type: spring-frontend
    env: dev